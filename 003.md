```c
#include "stdio.h"
int i=5
int main()
{
    
    while(i)//判断i是不是0
    {
        printf("%d\n",i);  
      i=i-1  
    }
    
    
}
```

一，循环
1，while语句的基本结构
while(表达式) 语句

3，i--和--i的区别

i-- 先取值后减1

--i 先减1后取值

4，无限循环 while(1)
5,do语句
6，do{语句} while(表达式)
7,设计一个C程序，计算用户输入的整数的位数
8，for语句  for(表达式1；表达式2；表达式3) 语句

表达式1只执行一遍 

看表达式2是不是真的

```c

int i=5;

int main()
{
    for(i=5;i>0;i--  )//三个表达式
    {
       printf("%d\n,i") 
     }
    
    
    
}
```



9，编写一个简单的C程序,在屏幕上显示5到1
10，for语句中的省略表达
11，C99中的for语句表达式1
12，逗号运算符
13，退出循环break

//break会跳转到这里

14，break语句跳转到循环结束之后
15，设计一个C代码，判断用户输入的数是不是素数

```c
int num;
int main()
{
    printf ("请输入一个整数：");
    scanf(“%d,&num);
    for(i=2;i<num;i++)
        if((num/i)==0)
            break;
    
    
}
if（i<num)
    printf("这个数是合数“）
    else
       printf（“这个数是素数”）
```



16，continue语句

```c
{
    printf("1");
    printf("2");
    continue;
    printf("3");
    printf("4");
//continue语句跳转到这里   
}
//Break跳转到循环体的末尾之后执行
```





17，goto语句
标识符： 语句
goto 标识符;     跳转 eg:goto b;直接跳到b 不会执行a

```c
a:
printf("这是a");
b:
printf("这是b")；
c:
printf("这是c")；
```



18，空语句

用；来表示一个语句

在写for的三个语句的时候可以用；代替