1.\+字母组成转义序列·

2.\n是换行 在两个代码之间

#include <stdio.h>

int main(int argc,char*argv[]){

printf(hello micu\nhello may)

return 0;

}

3.声明一个整数a int a

 声明一个小数b float b

声明放在使用之前(变量)

声明：int x=2

​           float y=3.14

使用:x=2

​        y=1.23

4.变量运算

*乘

/除

%取余

eg.设计c语言求长方体体积

(1)int lenth;

int width;

int high;

int volum;

volum=lenth*width*high;

输出函数printf("体积=%d",volum);

int类型的占位符%d

printf("体积：%d,volum")

(2)输入函数scanf("%d,&lenth");

scanf(%d,&width);

scanf(%d,&high);

volum=lenthwidthhigh;

printf("体积：%d", volum);

(3)完善一下程序

1,printf("请输入物体的长：");

scanf(%d,&lenth);

printf("请输入物体的宽：");

scanf(%d,&lenth);

printf("请输入物体的高：");

scanf(%d,&lenth);

printf("物体的体积是：%d", volum);

5.注释

多行注释      开头/*  注释内容   */结尾

单行注释      //注释内容

6.预处理指令

1）#include

2)#define  宏定义

#define PI 3.14f  前面的等价后面的

m=3.14f*r*r; 等价 m=PIrr;

7变量声明

1)int 整数  float 小数

2)变量命名规则：

只能包含英文字母，数字，下划线  temputure_2

不能以数字开头，必须以英文字母或者下划线开头  3val _val val3 val_3

不能和关键字重复  main int return

区分大小写  May mAy maY may MAY MAy May

3)可以一次声明多个，中间用逗号隔开

int a,b,c;  int a=1,b=2,c=3;  int a,b=2,c;

三，格式化输入/输出

1)printf函数  函数  f(x)=x^2  f(2)=4

2)printf("格式串",参数1，参数2，参数3………);

3)占位符 %d  %f  转义序列 \n

4)int a=3;float b=3.14;

printf("a=%d\nb=%f",a,b);

5)%shtu  %n.mf  n代表最小栏宽，如果不足将会左侧补空格，超过则正常显示

前者m代表显示有效位数，不足补0，超过则正常显示

后者表示小数点后有效位数，默认6

6)占位符与参数不匹配

参数多于占位符  参数少于占位符

数据类型不匹配

7)scanf函数

scanf("格式串",参数1，参数2，参数3) ;

9)int a;float b;

scanf("%d%f",&a,&b);

scanf("a=%d,b=%f",&a,&b);

一般不改变变量值的直接调用变量  一般改变变量值的需要取地址&