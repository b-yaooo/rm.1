int型和float型的结果是float 

%操作（取余）不能是浮点数只能是整数

/和%用作负数时 结果难以确定

一、表达式

3，运算符的优先级和结合性

int a,b,c;
a+b*c = a+(b*c)
无需纠结，直接加()

4.赋值运算符

把=右边的值赋给左边
称为左值

只有出现在等号左边才会被改变

5，复合赋值

后置++，先用值，后加1

完整代码

重点：b++ 是后置自增

完整代码

```c#include "stdio.h"
int a=2;
int b=3;
int c=4;
int main()
{
    a=b++;
    printf("a=%d\nb=%d",a,b);
    return 0;
}
重点：b++ 是后置自增
```

a=3 b=4

前置 ++b 规则：先加1，再拿去运算

初始 b=3

1. ++b：b先+1 → b=4

2. 再把b的值4赋值给a → a=

   总结：

   a = b++; 后置++：先用原值，后自增 → a=3，b=4

   • a = ++b; 前置++：先自增，再用新值 → a=4，b=4

1，关系运算符

<  >=  <=
结果只有真假 真1 假0
6<7是1
6>7是0
2，判等运算符
== 等于等于 !=不等于
5==5 是1     正确
5!=5是0       错误

3，逻辑运算符
             或且非
!  逻辑非

 &&逻辑与

   ||逻辑或


真 && 真 =真
真 && 假 =假
假 && 假 =假

真 || 真 =真
真 || 假 =真
假 || 假 =假

! 真 &&真=假

```c
#include "stdio.h"

int a=2;
int b=3;
int c=4;

int main()
{
    a= (b<c) && (c==5);
    printf("a=%d",a);
    return 0;
```

if语句
if(表达式)
语句
else
语句

设计一个C程序，提示用户输入一个数，并判断这个数是奇数还是偶数，输出结果

```c
#include "stdio.h"
int num;
int main()
{
    printf("请输入一个数：");
    scanf("%d",&num);
    if(num%2==0)
    {
        num=num*5;
        printf("这是一个偶数%d",num);
    }
    else
    {
        num=num*3;
        printf("这是一个奇数%d",num);
    }
    return 0;
}
```

条件表达式
if(a)b;else c;

表达式1? 表达式2: 表达式3
如果表达式1为真，就选择表达式2，否则就选择表达式3

10,布尔值
int

 float

 _Bool逻辑
int a;
float b;
_Bool c;      0就是0     其他数值都是1

0是假的 非0是真的

11，switch语句
找到匹配的常量，开始顺序往后执行
switch(表达式)
}

break 

```c
switch(num)
{
   case 1;
       printf("你的成绩不合格");
       break;
   case 2;
        printf('你的成绩不合格')；
        break;
        
        
}
```

 ```c
 switch(num)
 {
    case 0;
    case 1;
    case 2;
    case 3;
        printf("你的成绩不合格")
        break;
         
         
         
         
         
 }
 ```

